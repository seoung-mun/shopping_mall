# shoppingMall backend

Spring Boot REST API (JPA + PostgreSQL + Spring Security/JWT). Spring project itself
lives in `shoppingMall/` (this folder's subfolder, matches Gradle project name).

Design doc: `spec/2026-08-03-backend-design.md`
