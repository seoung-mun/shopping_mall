services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: shopping_mall
      POSTGRES_USER: shopping_mall
      POSTGRES_PASSWORD: shopping_mall
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
